class Solution:
    def canAliceWin(self, nums: List[int]) -> bool:    
        nums.sort()
        for k in nums:
            if k in range(10):
                s+=k
            else:break
        if s==sum(nums)-s:return False
        else:return True
                
        
        
